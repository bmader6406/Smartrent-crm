<div id="top-nav" class="navbar navbar-inverse navbar-fixed-top">
  <div class="container-fluid">
    <div class="navbar-header">
      <button data-target=".navbar-collapse" data-toggle="collapse" class="navbar-toggle collapsed" type="button">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a href="<%= raw main_app.root_path %>" class="navbar-brand">
        <i class="fa fa-caret-down"></i>
        <img src="https://s3.amazonaws.com/m.hy.ly/hyly/new_hyly_sm.png" alt="Hy.ly" />
        <span> 
          CRM
        </span>
      </a>
    </div>
    <div class="navbar-collapse collapse">
      <div class="collapse navbar-collapse">
        <ul class="nav navbar-nav">
          <li class="dropdown" id="prop-dd-nav">
            <div class="list-filter dropdown-menu">
          	  <form class="form-inline">
                <input type="text" class="form-control" placeholder="Start typing property name...">
              </form>

          	  <div class="scroller">
            	  <div class="list-group">
            	    <div class="divider"></div>
          	    
            	    <%
            	      properties_region = current_user.managed_properties.includes(:region).group_by{|p| p.region }
            	      properties_region.keys.each do |region|
            	    %>
            	      <b data-val="<%= region.id if region %>" data-label="<%= region.name if region %>"> <%= region ? region.name : "Other Metro" %></b>
              	    <div class="items">
                	    <% properties_region[region].each do |prop| %>
                	      <a href="<%= main_app.property_path(prop) %>" class="list-group-item" data-val="<%= prop.id %>" data-label="<%= prop.name %>"> <%= prop.name %> </a>
                	    <% end %>
              	    </div>
            	    <% end %>
            	  </div>
            	</div>
            </div>
          </li>
        </ul>

        <ul class="nav navbar-nav navbar-right">
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown"><%= current_user.full_name %> <i class="fa fa-angle-down fa-lg"></i></a>
            <ul class="dropdown-menu">
              <li>
                <a href="<%= main_app.profile_path %>">
                  Profile
                </a>
                <a href="<%= main_app.logout_path %>">
                  <i class="fa fa-sign-out"></i>
                  Logout
                </a>
              </li>
            </ul>
          </li>
        </ul>
      </div>
    </div><!--/.nav-collapse -->
  </div>
</div>

<script type="text/javascript">
  $(function(){
    App.setupDropdownNav();
  });
</script>