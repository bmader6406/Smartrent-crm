$('#analyze-container').replaceWith("<%= j(render 'send_summary')%>");
$("#report-header .export").attr("href", "<%= raw export_send_summary_reports_path(@property, :range => params[:range]) %>");

sendWrapAutoheight();